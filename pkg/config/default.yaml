settings:
  diagnosticsPort: "2112"
  log:
    level: "error"
    disableColors: "false"
  informersResyncPeriod: "30m"

  systemConfigMap:
    name: botkube-system
    namespace: botkube

plugins:
  cacheDir: "/tmp"

analytics:
  disable: false
