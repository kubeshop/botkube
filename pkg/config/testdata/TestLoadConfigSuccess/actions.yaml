actions:
  'show-created-resource':
    enabled: true
    displayName: "Display created resource"
    command: "kubectl get {{.Event.ResourceType}} -n {{.Event.Namespace}} {{.Event.Name}}"
    bindings:
      sources:
        - k8s-err-events
      executors:
        - kubectl-read-only
